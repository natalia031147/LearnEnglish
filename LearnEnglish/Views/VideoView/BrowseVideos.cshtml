
@{
    ViewBag.Title = "Videos";
}

<div div="cards-list"  ng-controller="BrowseVideosController" ng-init="getVideos();">
    
    <div class='md-padding' layout="row" layout-wrap>        
        <md-card flex-xs="" flex-gt-xs="30" ng-repeat="video in videos">
            <a ng-click="gotovideo(video.Id)" href="@Url.Action("GetVideo","Video")/{{video.id}}"><img ng-src="{{video.thumbnail}}" class="md-card-image"></a>
            <level level="video.level" style="margin-top: 5px; margin-left: 15px;"></level>
            <md-card-title>
                <md-card-title-text>
                    <a href="@Url.Action("GetVideo","Video")/{{video.id}}"><span class="md-headline" ng-click="loadVideo()">{{video.title}}</span></a>
                </md-card-title-text>
            </md-card-title>
            <hr style="width: 100%;margin-left: 0px; margin-bottom: 0px; background-color:black">
            <md-card-actions layout="column" layout-align="start">
                <md-button href="@Url.Action("Listening", "Video")/{{video.id}}">Listening<img class="done-action" src="~/Images/Done.png" ng-if="video.listeningModulePassed == true" width="25" height="25" /></md-button>
                <md-button href="@Url.Action("Writing","Video")/{{video.id}}">Writing<img class="done-action" src="~/Images/Done.png" ng-if="video.writingModulePassed == true" width="25" height="25" /></md-button>
            </md-card-actions>
        </md-card>
    </div>
</div>

